package classes.gameObjects;

import javafx.geometry.Point2D;
import javafx.scene.image.Image;

public class Flame extends GameObject {
    public Flame(Image texture, Point2D centre) {
        super(texture, centre);
    }
}
